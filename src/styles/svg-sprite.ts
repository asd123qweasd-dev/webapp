import { css } from "@emotion/react";

export const svgSprite = css`
  .svg-sprite, .svg-sprite svg {
    position: absolute;
    width: 0;
    height: 0;
    overflow: hidden;
  }
  
  .svg-icon {
    width: 1em;
    height: 1em;
  }
`
